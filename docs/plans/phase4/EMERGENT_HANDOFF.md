# Emergent handoff

Frozen for later visual work:

- Tokens: `frontend/src/design-system/tokens.js` and `--pp-*` variables.
- Routes: `frontend/src/design-system/routes.js` plus the existing `/` and `/perchpoint/:roleId/:viewId` paths.
- States: `frontend/src/design-system/states.js`.
- Components: `frontend/src/design-system/components.jsx`.
- Breakpoints: 320, 480, 768, 1024, 1280, 1440, and 1920.
- Themes: public `hawkvision`; internal `dark` by default, plus `light` and `system`.
- Density: `comfortable` and `compact`.

Visual work may not change schemas, authorization, API contracts, migrations, or domain state machines.
